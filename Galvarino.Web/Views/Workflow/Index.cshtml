@{
    ViewData["Title"] = "Mis Solicitudes";
}

@section breadcumbs{
    <li><a href="#"><i class="demo-pli-home"></i></a></li>
    <li>Etapas</li>
    
    @if((string)ViewData["etapaIn"] != "")
    {
        <li>@ViewData["Title"]</li>
        <li class="active">@ViewData["etapaIn"]</li>
    }
    else
    {
        <li class="active">@ViewData["Title"]</li>
    }
}

@section script{
     <!--Page Script-->
    <environment include="Development">
        <script src="~/js/mis-solicitudes.js"></script>
    </environment>
    <environment exclude="Development">
        <script src="~/js/mis-solicitudes.min.js"></script>
    </environment>
}



<table id="tabla-generica" data-toggle="table"
                data-url="/api/wf/v1/mis-solicitudes"
                data-search="true"
                data-show-refresh="false"
                data-show-toggle="false"
                data-show-columns="false"
                data-sort-name="id"
                data-page-list="[]"
                data-page-size="20"
                data-pagination="true" data-show-pagination-switch="false">
    <thead>
        <tr>
            <th data-field="expediente.credito.folioCredito" data-align="center" data-sortable="true">Folio Crédito</th>
            <th data-field="expediente.credito.rutCliente" data-align="center" data-sortable="true">Rut Afiliado</th>
            <th data-field="expediente.credito.tipoCredito" data-sortable="true" data-align="center" data-formatter="formatoTipoCredito">Tipo Crédito</th>
            <th data-field="expediente.credito.fechaDesembolso" data-align="center" data-sortable="true" data-formatter="formatoFecha">Fecha Colocación</th>
            <th data-field="tarea.etapa.nombre" data-align="center" data-sortable="true">Etapa</th>
        </tr>
    </thead>
</table>